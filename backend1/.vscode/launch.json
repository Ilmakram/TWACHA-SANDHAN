{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Train Model",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/train.py",
      "args": ["--data_dir", "backend1/IMG_CLASSES", "--model", "ResNet50", "--use_cuda"],
      "console": "integratedTerminal"
    }
  ]
}
